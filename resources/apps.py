from django.apps import AppConfig


class ResourcesConfig(AppConfig):
    """Configuration for the team resources app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'resources'
